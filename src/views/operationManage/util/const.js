let LOGIN_ROUTER = '/admin#/login';
let TOLOGIN = 'http://' + window.location.host + window.context + 'home/login'
let TOHOME = 'http://' + window.location.host + window.context + 'home'
let LOGINOUT = 'http://' + window.location.host + window.context + 'platform/logout'
let BASEURL = 'http://' + window.location.host + window.context
let BASEURLHOST = 'http://' + window.location.host

export {
  LOGIN_ROUTER,
  TOLOGIN,
  TOHOME,
  LOGINOUT,
  BASEURL,
  BASEURLHOST
}
